spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monitor_test
    username: monitor
    password: monitor
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    clean-disabled: false
