# Non-secret configuration, as environment variables (see the README "Configuration" section).
# Credentials go in the monitor-db Secret, never here.
DB_URL=jdbc:postgresql://postgres:5432/monitor
MONITOR_DEFAULT_M=0
MONITOR_DEFAULT_S=0
